{
  "name": "task-manager-local",
  "version": "1.6.1",
  "private": true,
  "description": "Offline-first scheduled folder collection for local networks",
  "type": "module",
  "scripts": {
    "dev:server": "node server/index.js",
    "dev:ui": "vite",
    "build": "vite build",
    "start": "node server/index.js",
    "check": "node --check server/index.js && node --check server/network-browser.js && node --check server/scheduler.js && node --check server/file-ops.js && node --check server/store.js && node --check server/schedule.js && node --check server/validation.js"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "express": "5.2.1",
    "react": "19.2.1",
    "react-dom": "19.2.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "6.0.5",
    "vite": "8.2.1"
  }
}
